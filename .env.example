# Copy to .env.local and fill in real values. .env.local is gitignored.

# Server-only: the browser never sees this. All backend calls go through
# Next.js route handlers (src/app/api/**) which proxy to this URL.
API_BASE_URL=

# Server-only: used in route handlers / server components, not exposed to the browser
API_KEY=
